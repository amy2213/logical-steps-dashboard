{
  "name": "logical-steps-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "check": "npm run build && CI_BUILD_ASSERT=1 npm run test",
    "deploy": "npm run build && gh-pages -d dist"
